# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Trashy' do
  use_frameworks!

  pod 'BarcodeScanner'
  pod 'Hero'
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'

end
